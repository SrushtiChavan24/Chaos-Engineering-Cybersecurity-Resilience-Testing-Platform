apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: target-app-pod-kill
  namespace: default
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - default
    labelSelectors:
      app: target-app
  duration: "10s"
